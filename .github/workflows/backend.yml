<<<<<<< HEAD
<<<<<<< HEAD
name: Backend-dev
=======
name: Backend CI/CD — Dev & Prod
>>>>>>> dev
=======
name: Backend CI/CD — Dev
>>>>>>> dev

on:
  push:
    branches:
      - dev
<<<<<<< HEAD
      - main
<<<<<<< HEAD
=======
    paths:
      - 'backend-node/**'          # Only run if backend changes
      - '.github/workflows/backend.yml'
>>>>>>> dev

jobs:
  deploy:
=======
    paths:
      - 'backend-node/**'

jobs:
  backend-dev-deploy:
>>>>>>> dev
    runs-on: self-hosted

    steps:
      - name: 🛎 Checkout code
        uses: actions/checkout@v3

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

<<<<<<< HEAD
<<<<<<< HEAD
      # ==================================
      # Detect branch + set environment
      # ==================================
      - name: ⚙️ Set environment paths
        run: |
          BRANCH=$GITHUB_REF_NAME
          echo "Detected branch: $BRANCH"
=======
      # =========================
      # Set branch-specific paths
      # =========================
      - name: ⚙️ Set branch environment
        run: |
          BRANCH=$GITHUB_REF_NAME
          echo "Branch detected: $BRANCH"
>>>>>>> dev

          echo "BACKEND_PATH=$GITHUB_WORKSPACE/backend-node" >> $GITHUB_ENV

          if [ "$BRANCH" = "dev" ]; then
            echo "BACKEND_SERVICE=backend-dev.service" >> $GITHUB_ENV
          elif [ "$BRANCH" = "main" ]; then
            echo "BACKEND_SERVICE=backend-prod.service" >> $GITHUB_ENV
          else
            echo "Unknown branch: $BRANCH"
            exit 1
          fi

<<<<<<< HEAD
          echo "Environment set."
          echo "BACKEND_PATH=$BACKEND_PATH"
          echo "BACKEND_SERVICE=$BACKEND_SERVICE"

      # ==================================
      # Backend build (install only)
      # ==================================
      - name: 📦 Install backend dependencies
        run: |
          echo "Installing backend dependencies..."
          cd $BACKEND_PATH
          npm install

      # ==================================
      # Restart backend
      # ==================================
      - name: 🚀 Restart backend service
        run: |
          echo "Restarting backend systemd service: $BACKEND_SERVICE"
          sudo systemctl stop $BACKEND_SERVICE || true
          sudo systemctl start $BACKEND_SERVICE
          sudo systemctl status $BACKEND_SERVICE --no-pager

=======
      # =========================
      # Install backend dependencies
      # =========================
=======
>>>>>>> dev
      - name: 📦 Install backend dependencies
        run: |
          cd backend-node
          npm install

      - name: 🚀 Restart backend dev service
        run: |
<<<<<<< HEAD
          echo "Restarting $BACKEND_SERVICE"
          sudo systemctl restart $BACKEND_SERVICE
          sudo systemctl status $BACKEND_SERVICE --no-pager
>>>>>>> dev
=======
          sudo systemctl restart backend-dev.service
          sudo systemctl status backend-dev.service --no-pager
>>>>>>> dev
